import { APP_NAME } from "../../../app-constants";

export const MODULE_NAME = "MODAL";
const PREFIX = `${APP_NAME}/${MODULE_NAME}`;

export const OPEN_POPUP = `${PREFIX}/OPEN_POPUP`;

export const CLOSE_POPUP = `${PREFIX}/CLOSE_POPUP`;
