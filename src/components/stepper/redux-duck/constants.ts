import { APP_NAME } from "../../../app-constants";

export const MODULE_NAME = "STEPPER";
const PREFIX = `${APP_NAME}/${MODULE_NAME}`;

export const REGISTER_STEPS = `${PREFIX}/REGISTER_STEPS`;
export const REGISTER_STEPS_DONE = `${PREFIX}/REGISTER_STEPS_DONE`;

export const ALLOW_NEXT_STEP = `${PREFIX}/ALLOW_NEXT_STEP`;
export const ALLOW_NEXT_STEP_DONE = `${PREFIX}/ALLOW_NEXT_STEP_DONE`;
